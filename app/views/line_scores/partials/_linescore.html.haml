-# %tr
-#   %td.name= team_name(game, team)
-#   - innings(game, team).each do |inning|
-#     %td.innings= inning["runs"]
-#   %td.totals= game["game"][team]["runs"]
-#   %td.totals= game["game"][team]["hits"]
-#   %td.totals= game["game"][team]["errors"]
%thead
  - game["game"]["home"]["scoring"].each do |inning|
    %td.inning= inning["number"]
%tbody
  %tr
    - game["game"]["away"]["scoring"].each do |inning|
      %td.inning= inning["runs"]
  %tr
    - game["game"]["home"]["scoring"].each do |inning|
      %td.inning= inning["runs"]
